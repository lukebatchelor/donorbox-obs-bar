{
  "name": "donorbox-obs-bar",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "netlify-lambda build functions",
    "dev-backend": "yarn netlify-lambda serve functions",
    "dev": "http-server -c-1 --proxy http://localhost:9000 ./site"
  },
  "dependencies": {
    "http-server": "^0.12.0",
    "netlify-cli": "2.12.0",
    "netlify-lambda": "^1.6.3"
  }
}
